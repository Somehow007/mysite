<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.somehow.mysite.dao.mapper.ArticleTagMapper">
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO t_article_tag (id, article_id, tag_id, create_time, update_time, del_flag)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.articleId}, #{item.tagId}, #{item.createTime}, #{item.updateTime}, 0)
        </foreach>
    </insert>
</mapper>
